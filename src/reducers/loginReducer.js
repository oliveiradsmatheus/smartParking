/*const inicialState = "";

// Reducer para lidar com as ações de logar e deslogar
const loginReducer = (state = inicialState, action) => {
  switch (action.type) {
    case "LOGAR":
      return action.payload; // Armazena o nome do usuário no estado
    case "DESLOGAR":
      return ""; // Limpa o estado ao deslogar
    default:
      return state;
  }
};

export default loginReducer;*/
